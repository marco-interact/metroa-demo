# Metroa Backend - Python Dependencies
# Optimized for RunPod deployment

# Web Framework
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Database
aiosqlite==0.20.0

# Computer Vision & 3D Processing
opencv-python==4.12.0.88
numpy==1.26.4
scipy==1.13.0  # Required for trimesh operations
open3d==0.19.0  # Point cloud post-processing (cleaning, outlier removal, downsampling)
trimesh==4.5.0  # Mesh processing and GLB conversion

# Enhanced 3D Processing (Open3D sufficient for backend processing)
# Real-time streaming handled via FastAPI WebSocket if needed

# GPU Acceleration
# cupy-cuda12x==12.3.0  # GPU-accelerated array processing (optional - heavy build)
# numba==0.58.1  # JIT compilation for GPU operations (optional - complex build)

# Utilities
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.0

# Optional: Monitoring (for production)
# psutil==6.1.0


